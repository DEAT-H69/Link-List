# Link-List
This Python program generates a 12 multiplication table in decreasing order (from 12×50 = 600 down to 12×1 = 12) using a singly linked list. Each number in the table is stored as a node in the linked list, and the program prints the numbers in a formatted table layout with a specified number of columns per row.
